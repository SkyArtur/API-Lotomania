"""
Testes da aplicação `api`: exceptions, os serviços de criação
(`api.services.creates`), os orquestradores de registro (`api.services`) e,
em `test_api_views_*`, o contexto HTTP das viewsets (URL, permissão,
serializer e view) via `APITestCase`.
"""
